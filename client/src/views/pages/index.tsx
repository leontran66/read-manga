import { Fragment } from 'react';

import Landing from '../components/Landing';

const IndexLayout = () => {
  return (
    <Fragment>
      <Landing />
    </Fragment>
  );
};

export default IndexLayout;