import { Fragment } from 'react';

import Profile from '../components/Profile';

const ProfileLayout = () => {
  return (
    <Fragment>
      <Profile />
    </Fragment>
  );
};

export default ProfileLayout;